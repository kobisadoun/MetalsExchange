<resources>

   <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/metalsexchange_color</item>
        <item name="colorPrimaryDark">@color/metalsexchange_dark_color</item>
    </style>

    <!-- Main activity theme. -->
    <style name="MetalsExchangeTheme" parent="AppTheme">
        <item name="actionBarStyle">@style/ActionBar.Solid.MetalsExchange.NoTitle</item>
    </style>

    <!-- Settings activity theme. -->
    <style name="SettingsTheme" parent="AppTheme">
    </style>

    <!-- main activity action bar styles -->
    <style name="ActionBar.Solid.MetalsExchange.NoTitle" parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="displayOptions">useLogo|showHome</item>
        <item name="logo">@drawable/ic_logo</item>
    </style>

    <!-- Style for rates listview -->
    <style name="RatesListStyle">
        <!-- Here it's a stub.  In screens of sufficient width, this style includes modifications
        for two-pane layout -->
    </style>

</resources>